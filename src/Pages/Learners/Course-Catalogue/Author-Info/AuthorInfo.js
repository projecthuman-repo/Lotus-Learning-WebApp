// Import necessary modules and components
import React from 'react';

// Define the functional or class component
const MyComponent = () => {
  // Component logic and state go here

  return (
    // JSX elements representing the component's UI
    <div>
      <h1>Hello!</h1>
    </div>
  );
};

// Export the component to make it available for use in other files
export default MyComponent;
